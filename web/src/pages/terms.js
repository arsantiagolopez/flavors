import { Flex } from "@chakra-ui/react";
import React from "react";

const TermsPage = () => {
  return <Flex {...styles.wrapper}>These are Flavors' terms of services</Flex>;
};

export default TermsPage;

// Styles

const styles = {
  wrapper: {},
};
