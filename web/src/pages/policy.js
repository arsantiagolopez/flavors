import { Flex } from "@chakra-ui/react";
import React from "react";

const PolicyPage = () => {
  return <Flex {...styles.wrapper}>This is Flavors' Policy.</Flex>;
};

export default PolicyPage;

// Styles

const styles = {
  wrapper: {},
};
