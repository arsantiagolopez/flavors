import { Flex, Heading } from "@chakra-ui/react";
import React from "react";

const Sell = () => {
  return (
    <Flex {...styles.wrapper}>
      <Heading>Sell</Heading>
    </Flex>
  );
};

export { Sell };

// Styles

const styles = {
  wrapper: {},
  sidebar: {},
  content: {},
};
