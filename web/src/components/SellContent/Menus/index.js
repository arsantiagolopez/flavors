import { Flex } from "@chakra-ui/react";
import React from "react";

const Menus = () => {
  return <Flex {...styles.wrapper}></Flex>;
};

export { Menus };

// Styles

const styles = {
  wrapper: {},
};
