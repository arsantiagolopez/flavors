import { Flex } from "@chakra-ui/react";
import React from "react";

const Listings = () => {
  return <Flex {...styles.wrapper}></Flex>;
};

export { Listings };

// Styles

const styles = {
  wrapper: {},
};
